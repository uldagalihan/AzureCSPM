"""
Shared utilities for Azure CSPM.

Modules:
    auth        — OAuth 2.0 token acquisition (client-credentials flow).
    arm_parser  — Azure Resource Manager (ARM) ID parsing helpers.
    graph       — Microsoft Graph API principal display-name resolution.
    azure_api   — Common Azure REST helpers (subscription listing, pagination).
"""
